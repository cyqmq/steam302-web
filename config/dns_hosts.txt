# steam302-web 自定义 DNS 解析规则（可选）
#
# 每行格式: <域名或 *.域名> <IP>
#   *. 前缀表示该域名及其任意子域都按此 IP 应答（优先于默认应答 IP）。
# 行首 # 或空行被忽略。启用开关: 设置 -> DNS 重定向 -> 使用用户自定义规则。
#
# 示例（取消注释生效）:
# *.example.com     1.2.3.4
# myhost.local      10.0.0.5